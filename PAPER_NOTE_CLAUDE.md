# 論文筆記格式規範 — Paper Note Guidelines

每次整理 AI / 技術論文筆記，必須遵循以下結構。核心原則：**先讓人看懂「它在做什麼」，再深入「為什麼這樣做」。**

---

## 整體結構順序

```
1. Frontmatter
2. TL;DR（一句話）
3. 核心演算法直覺（白話 + 流程圖）
4. 具體數字範例
5. 關鍵圖表解讀
6. 研究背景與動機（為什麼需要這個）
7. 方法細節
8. 實驗結果
9. 侷限與風險
10. 延伸應用
11. 關鍵數據速查表
```

> [!note] 設計邏輯
> 傳統學術順序（背景 → 方法 → 實驗）適合審稿人，不適合快速吸收。把「它在做什麼、效果多好」放最前，讓讀者在 2 分鐘內決定要不要繼續深讀。

---

## 1. Frontmatter（必填）

```yaml
---
title: 中文標題（論文名稱直譯或意譯皆可）
date: YYYY-MM-DD（arXiv 發布日期）
source: 會議/期刊名稱（如 ICLR 2026、arXiv）
authors: 第一作者 et al.（機構）
sector: 研究領域（如 LLM 推理 / 向量量化 / RL）
tags:
  - 核心技術1
  - 核心技術2
url: arXiv 或論文連結
---
```

---

## 2. TL;DR（必寫，放在標題後）

用 `[!abstract]` callout，一句話包含：
- **核心方法名稱** + **核心做法** + **最重要的結果數字**

格式：
```markdown
> [!abstract] 一句話總結
> XXX 用「[核心做法]」，在不需要[限制條件]的情況下，達到[關鍵結果+數字]。
```

---

## 3. 核心演算法直覺（必寫，放在研究背景之前）

### 3.1 白話解釋（2–4 段）

- 假設讀者是工程師，不假設讀者看過論文
- 用生活比喻或類比打底（會議紀錄、行李箱、打散再收納）
- 說清楚「它跟直接做有什麼不一樣」

### 3.2 演算法流程（必寫）

用 code block 寫出端到端的資料流，格式：

```
輸入 → 步驟 A → 步驟 B → 步驟 C → 輸出
```

每個步驟一行，括號內說明「為什麼這樣做」。範例：

```
輸入向量 x
→ 隨機旋轉 Πx        （讓各維度近似獨立，消除 outlier 影響）
→ 逐維 Lloyd-Max 量化  （對每個座標找最近 centroid，存 b-bit index）
→ 殘差 r = x - x̃     （MSE 修復後還有內積偏差需要補正）
→ 1-bit QJL(r)       （修正內積偏差，確保 unbiased 估計）
→ 輸出：index + sign vector + residual norm
```

### 3.3 與舊方法最大的不同（必寫，1–3 點）

用對比格式：

| | 舊方法 | 本文方法 |
|--|--------|---------|
| 關鍵差異1 | ... | ... |
| 關鍵差異2 | ... | ... |

---

## 4. 具體數字範例（必寫，放在方法細節之前）

**目的：讓人感受到壓縮/加速/改善的量級，不是只看百分比。**

必須包含：
- 至少一個「端到端的數字範例」（輸入 → 輸出，帶單位）
- 不同設定下的對比（如不同 bit-width 的 byte 數字）

格式範例：

```
128-channel FP16 向量：
  原始大小 = 128 × 16 = 2048 bits = 256 bytes

壓縮成 2.5-bit（混合）：
  32 outlier channels × 3 bits + 96 channels × 2 bits = 288 bits = 36 bytes
  壓縮比 ≈ 7.1×

10,000 token KV cache：
  FP16：2.56 MB → 2.5-bit：0.36 MB（節省 86%）
```

---

## 5. 關鍵圖表解讀（有圖就必寫）

每張重要圖表一個條目，格式：

```markdown
### 圖 N（頁碼）：圖表標題

**看什麼**：這張圖在比較什麼 / 展示什麼趨勢
**關鍵發現**：一句話說最重要的 takeaway
**數字**：圖中最重要的數值
```

> [!note] 哪些圖必須解讀
> - 方法架構圖（如果有）
> - 主要對比實驗圖表
> - 誤差 / 偏差 / 分布圖（理解方法正確性）
> - ablation 結果圖
>
> 裝飾性圖片、示意圖不需要解讀。

---

## 6. 研究背景與動機

- 說清楚這個領域的「缺口」（gap），用條列格式
- 不超過 4 個 gap，每個 gap 一行
- 說明本文欲解決的具體痛點

---

## 7. 方法細節

- 適合想深讀的人
- 涵蓋：超參數、公式、演算法變體（如 mse 版 vs prod 版）
- 可以有公式，但每個公式後面要有一句白話解釋

---

## 8. 實驗結果

- 用表格呈現主要對比數字
- 每張表格下方必須有一行 `> 重點：...` 說明最重要的 takeaway
- 包含：baseline 設定、主要指標、與 SOTA 的差距

---

## 9. 侷限與風險

用 `[!warning]` callout，至少 2 點：
- 理論假設不成立的情境
- 工程落地的挑戰
- 哪些任務 / 資料集上效果有限

---

## 10. 延伸應用

- 短期：可以立刻套用的場景
- 中期：需要工程整合的方向
- 長期：未來研究可以怎麼延伸

---

## 11. 關鍵數據速查表（必寫，放在最後）

整理論文中所有出現的關鍵數字：

```markdown
## 附：關鍵數據速查

| 指標 | 數值 |
|------|------|
| ... | ... |
```

必須涵蓋：論文來源 / 核心對比數字 / 資料集規模 / 理論界限 / 速度 / 精度。

---

## Callout 工具表

| Callout | 用途 |
|---------|------|
| `[!abstract]` | TL;DR 一句話總結 |
| `[!info]` | 演算法流程、背景知識 |
| `[!tip]` | 工程含義、應用價值 |
| `[!note]` | 補充說明、判讀提示 |
| `[!warning]` | 侷限、風險、假設不成立的條件 |
| `[!danger]` | 論文核心錯誤風險 / 無法複現的問題 |
| `[!quote]` | 論文原文重要引用 |

---

## 語言規範

- **所有輸出一律使用繁體中文**
- 論文術語保留英文（KV cache、MSE、QJL、RLHF）
- 人名、機構名不翻譯（Zandieh、Google Research）
- 數字：帶單位（bytes、bits、秒、%）

---

## 品質標準

一篇好的論文筆記應該做到：

1. **2 分鐘可判讀**：TL;DR + 演算法直覺讓人決定要不要深讀
2. **有具體數字**：不只說「更快」，要說「快多少倍、省多少 bytes」
3. **有批判性**：明確說理論假設的邊界條件和侷限
4. **工程視角**：說清楚落地的挑戰（有沒有開源、GPU kernel 難度）
5. **有速查表**：所有數字集中在最後，不需要重讀全文
