# AI 技術筆記格式規範 — AI Tech Note Guidelines

每次整理 AI 技術筆記（課程筆記、論文閱讀、技術講座），必須遵循以下格式。
核心原則：**保留論述結構，不壓縮推理過程；圖表與原文並存，讓讀者能重建思路。**

---

## 筆記類型判斷

先判斷這份筆記屬於哪種類型，對應使用不同的主體結構：

| 類型 | 適用場景 | 結構特點 |
|------|---------|---------|
| **A. 課程 / 講座筆記** | 課程錄影、技術演講、教學系列 | 跟隨講者邏輯，主題導向，無固定章節 |
| **B. 論文閱讀筆記** | arXiv 論文、會議論文、技術報告 | 動機 → 方法 → 實驗 → 結論，學術結構 |
| **C. 混合型** | 有論文主體但加入大量背景解說 | 先課程背景，再論文拆解 |

---

## 1. Frontmatter（必填）

```yaml
---
title: 中文標題
date: YYYY-MM-DD（發布或整理日期）
source: 課程名稱 / 媒體 / 會議（如 NTU CSIE ML、arXiv）
speaker: 講者或第一作者（職稱或機構）
topic: 技術主題（如 LLM 評估 / Agentic Framework / KV Cache 壓縮）
tags:
  - 核心技術1
  - 核心技術2
url: 原始連結（影片、論文、投影片）
---
```

---

## 2. 摘要（必寫，放在標題後）

一段話（3–5 句），說明：
- 這份筆記在講什麼
- 核心問題是什麼
- 最重要的結論或洞見是什麼

**不用 callout 框，直接段落文字即可。**

---

## 3A. 課程筆記主體結構

### 跟隨講者邏輯，不強加章節框架

- 用 `#` H1 標記講者的**主要討論主題**（通常是大概念或問題）
- 用 `##` H2 標記**子主題或子問題**
- 用 `###` H3 標記**具體方法、案例、工具**

### 每個概念的標準寫法

```
- **概念名稱**：一句話定義
    - **子觀點 A**：說明內容
        - **具體例子**：...
    - **子觀點 B**：說明內容
        - **問題/限制**：...
        - **解決方案**：...
```

### 關鍵原則

1. **不只寫結論**：推理過程、類比說明、例外情況都要保留
2. **問題 → 解決方案**：每個概念優先回答「為什麼需要這個」
3. **對比必須完整**：舊方法 vs. 新方法、方法 A vs. 方法 B，不得壓縮成一句話
4. **圖在哪、筆記在哪**：截圖放在對應概念段落下方，不集中放在文末

---

## 3B. 論文筆記主體結構

### 固定章節順序

```
# 動機與貢獻
# Method Overview（含總覽表格 + 流程說明）
## 各子模組（Tool Cards / Planner / Executor / ...）
# Experiment
## 實驗設定
### Benchmark / 模型比較 / 驗證方式
## 結果
### 主結果 + 與其他方法比較
# Conclusion
```

### 方法說明必須包含

| 元素 | 說明 |
|------|------|
| 總覽表格 | 各子模組一行，功能摘要 |
| 端到端流程 | 用 code block 寫出資料流（見下方格式） |
| 每個模組的 I/O | 輸入是什麼、輸出是什麼、核心操作是什麼 |
| 與舊方法的差異 | 明確說「過去方法的問題在哪」 |

**資料流 code block 格式：**

```
輸入 Query → Planner（高階計畫）
→ Executor（低階指令 + 執行）
→ Context 更新（軌跡儲存）
→ Verifier（是否完成？）
→ 若未完成：回到 Planner 下一步
→ 若完成：Summarizer → 最終答案
```

### 實驗結果必須包含

- Baseline 設定（零樣本 / CoT / 其他框架）
- 主要指標（準確率、速度、壓縮比等）
- **表格**：數字對比，每個表格下方加一行重點說明
- 工具使用分佈（如果有）

---

## 4. 圖片處理規則

### 保留的圖片
- 方法架構圖、流程示意圖
- 實驗結果圖表（折線圖、長條圖、熱力圖）
- 對比表格截圖
- 演算法流程截圖
- 任何有數字、標籤、分析資訊的圖

### 刪除的圖片
- 無文字的純裝飾圖
- 重複的講者頭像、品牌 Logo

### 圖片放置原則

**圖片放在對應段落下方，緊接在解釋文字之後：**

```markdown
## 評估流程

標準化評估分為四個步驟：...

![評估流程四步驟示意](attachment:xxx.png)

1. 準備標準化測試集 (Benchmark)
2. 讓模型產生輸出
3. 用評估指標比較
4. 匯總得出性能指標
```

**圖片後可加說明行（斜體）：**

```markdown
![三階段工具選擇流程（Algorithm 1）](attachment:xxx.png)
*圖：貪婪式工具組優化，從 O(2ⁿ) 降至 O(n) 複雜度*
```

**Obsidian 本地附件格式：**
`![[Pasted image xxx.png]]` → 直接保留，放在對應段落

---

## 5. 表格使用規範

### 適合用表格的情境

| 情境 | 範例 |
|------|------|
| 子模組對比 | 各模組功能、I/O、限制 |
| 方法比較 | 舊方法 vs. 新方法 vs. 本文方法 |
| 實驗設定 | Baseline 設定、超參數 |
| 工具清單 | 工具名稱、功能、使用場景 |
| 類型分類 | 筆記類型、適用場景、結構特點 |

### 不適合用表格的情境
- 連續推理過程（用 code block 或條列）
- 單一維度的列表（直接用 bullet）

---

## 6. Code Block 使用規範

### 適合用 code block 的情境

| 情境 | 說明 |
|------|------|
| 資料流 / 演算法流程 | 端到端的步驟，每步一行 |
| JSON / 程式碼範例 | 論文中的具體實作 |
| 數字計算過程 | 壓縮比、效能計算 |
| 時間軸 / 因果鏈 | 事件順序或依存關係 |

**格式範例（資料流）：**

```
輸入向量 x（128-dim, FP16）
→ 隨機旋轉 Πx        （讓各維度近似獨立）
→ 逐維 Lloyd-Max 量化  （找最近 centroid，存 b-bit index）
→ 殘差 r = x - x̃     （補正內積偏差）
→ 輸出：index + sign vector
```

---

## 7. 重要概念標記規範

| 標記 | 用途 | 範例 |
|------|------|------|
| `**粗體**` | 關鍵術語、概念名稱、重要判斷 | `**古德哈特定律**` |
| `*斜體*` | 圖片說明行、補充說明 | `*圖：勝率回測結果*` |
| `` `code` `` | 函式名稱、工具名稱、技術縮寫 | `` `execute()` ``、`` `tool_name` `` |
| `（括號）` | 中文補充說明、英文對照 | `（如 Python、搜尋）` |
| 外部連結 | 影片、論文、官網 | `[LM Arena](https://lmarena.ai)` |

---

## 8. 課程筆記的特殊格式

### 「問題 → 解決方案」結構（必用於討論難題的段落）

```
- **問題**：...
    - **原因**：...
    - **解決方案**：...
        - **方法 A**：...
        - **方法 B**：...
```

### 案例說明結構

```
- **案例名稱**：一句話定位
    - **情境**：...
    - **結果 / 發現**：...
    - **啟示**：...
```

### 比較型內容（必用表格或對比列表）

```markdown
| | 方法 A | 方法 B |
|--|--------|--------|
| 優點 | ... | ... |
| 限制 | ... | ... |
| 適用場景 | ... | ... |
```

或用條列：

```
- **舊方法**：直接讓同一個 LLM 同時規劃與產生命令
    - **問題**：容易導致錯誤，規劃與執行混雜
- **本文方法**：分離 Planner 與 Executor
    - **優點**：低階指令錯誤不汙染高階計畫
```

---

## 9. 語言規範

- **所有輸出一律使用繁體中文**，無論原始資料語言
- 技術術語保留英文：`Benchmark`、`KV cache`、`LLM`、`RLHF`、`token per second`
- 人名、機構名不翻譯：`GPT-4o`、`OpenAI`、`AAAI`、`Anthropic`
- 數字帶單位：`58.5%`、`256 bytes`、`9.3% 提升`
- 論文標題保留英文縮寫：`OctoTools`、`BERTScore`、`BLEU Score`

---

## 10. 深度標準（防止過度壓縮）

**整理時必須保留的內容：**

1. **推理鏈**：為什麼 A 導致 B？機制是什麼？不能只寫「A 導致 B」
2. **對比完整性**：原文有 A vs. B，整理後不得壓縮成一句「A 比 B 好」
3. **具體數字**：所有出現的百分比、倍數、規模數字都要保留
4. **例外與限制**：作者提到的邊界條件、失效場景都要保留
5. **圖表對應文字**：每張重要圖旁邊要有說明文字，不能只貼圖

**深度判斷基準：**
整理後的篇幅應能讓讀者理解「為什麼作者這樣設計」，而不只是「作者做了什麼」。

---

## 11. 品質檢查清單

整理完成前，逐項確認：

- [ ] Frontmatter 所有必填欄位已填寫
- [ ] 開頭有摘要段落（不超過 5 句）
- [ ] 每個主要概念的「為什麼」有保留，不只有結論
- [ ] 所有重要圖片有放置（並在對應段落下方）
- [ ] 對比分析用表格或對比列表呈現，未壓縮成一句話
- [ ] 實驗數字完整（Baseline、本文方法、提升幅度）
- [ ] 技術術語保留英文，其餘使用繁體中文
- [ ] 結尾有「關鍵數據速查表」（如有數字型內容）

---

## 附：關鍵數據速查表格式（有數字時必寫）

```markdown
## 附：關鍵數據速查

| 指標 | 數值 | 備註 |
|------|------|------|
| OctoTools 平均準確率 | 58.5% | 16 個 benchmark 平均 |
| vs. Zero-shot 提升 | +9.3% | GPT-4o 底層模型 |
| 測試最大步驟數 | 10 步 | 推理預算上限 |
```
